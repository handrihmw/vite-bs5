{
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "sass": "^1.54.0",
    "vite": "^3.0.4"
  },
  "dependencies": {
    "bootstrap": "^5.2.0"
  }
}
